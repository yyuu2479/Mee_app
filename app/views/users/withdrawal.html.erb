<!--退会確認画面-->
<div class="withdrawal container mt-5">
  <div class="col-md-12 mx-auto">
    <h3 class="text-center"><%= @user.nickname %>さん、本当に退会しますか？</h3>
    <h4 class="text-center">退会する場合、登録メールアドレスとパスワードを入力して下さい</h4>
    <p class="text-center mt-5">
      <%= link_to '退会する', user_path(current_user.id), method: :delete, class: 'btn btn-danger btn-lg' %>
    </p>
  </div>
</div>